import React from "react";
import IPage from "../interfaces/ipage";

const Directory: React.FunctionComponent<IPage> = porps => {
    return (
        <div>
            <p>Hola soy el driector de la pelicula</p>
        </div>
    )
}

export default Directory;